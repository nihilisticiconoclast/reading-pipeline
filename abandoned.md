# Abandoned

Books started and given up on. The pipeline reads this as a *negative signal* —
it won't re-suggest these, and where a reason is given it steers away from books
with similar qualities. Add a short note on *why* it didn't land; that's the
useful part.

<!-- Format: - **Title** — Author (Year). Why abandoned. -->
